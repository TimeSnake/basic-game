package de.timesnake.basic.game.core;

import org.bukkit.event.Listener;

public class UserManager implements Listener {

    public UserManager() {
        // Server.registerListener(this, BasicBukkit.getPlugin());
    }
}
